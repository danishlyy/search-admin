
server:
  port: 8080

spring:
  application:
    name: search-admin
  profiles:
    active: dev3
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848
        file-extension: yaml
        enabled: true
